{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync": "bun run scripts/sync-book.ts",
    "dev": "bun run sync && astro dev",
    "start": "bun run dev",
    "build": "bun run sync && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.6",
    "@fontsource-variable/atkinson-hyperlegible-next": "^5.3.0",
    "astro": "^7.0.2",
    "sharp": "^0.34.5"
  }
}